# 과제 2 — 상품 카드 (Props)

`react-4차시-Props.md` 과제 2 결과물

## 실행

```bash
npm install
npm run dev
```

## 포인트

- `price.toLocaleString('ko-KR') + '원'` → `1,500,000원`
- `discount` 있으면 할인 배지 + 할인가 표시
- `discount` 없으면 정가만 표시
